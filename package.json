{
  "name": "steadydesk-it-website",
  "version": "1.0.0",
  "private": true,
  "description": "Framework-free HTML, CSS, and JavaScript website for managed IT support.",
  "engines": { "node": ">=22" },
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build": "node scripts/build.mjs",
    "start": "node scripts/dev.mjs",
    "test": "npm run build && node --test tests/site.test.mjs",
    "lint": "node --check site/assets/app.js && node --check server/index.js && node --check scripts/build.mjs && node --check config/services.mjs"
  },
  "dependencies": {},
  "devDependencies": {},
  "type": "module"
}
