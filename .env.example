# Required for automatic contact-form email delivery.
# Create the key in your Resend account, then add the same values to the hosted site.
RESEND_API_KEY=re_your_api_key
RESEND_FROM_EMAIL=SteadyDesk IT <requests@your-verified-domain.com>
